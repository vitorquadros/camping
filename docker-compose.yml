version: '3'

services:
  db:
    image: postgres
    environment:
      POSTGRES_PASSWORD: '8642'
    ports:
      - '5454:5432'
    volumes:
      - /home/containers/Docker-Compose/db:/var/lib/postgresql/data
